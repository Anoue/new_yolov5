# YOLOv5 馃殌 by Ultralytics, GPL-3.0 license

# Parameters
nc: 80  # number of classes
depth_multiple: 0.33  # model depth multiple
width_multiple: 0.50  # layer channel multiple
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 v6.0 backbone
backbone:
  # [from, number, module, args]
  [[-1, 1, Conv, [64, 6, 2, 2]],  # 0-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 1-P2/4
   [-1, 1, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 3-P3/8
   [-1, 1, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 5-P4/16
   [-1, 1, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 7-P5/32
   [-1, 1, C3, [1024]],
   [-1, 1, SPPF, [1024, 5]],  # 9
  ]

head:
  [[-1, 1, Conv, [512, 1, 1]], # 10 256 20 20 

  [10, 1, Split_1, [2]], # 11 128 20 20
  [10, 1, Split_2, [2]], # 12 128 20 20

  [6, 1, Split_1, [2]], # 13 128 40 40
  [6, 1, Split_2, [2]], # 14 128 40 40

  [4, 1, Split_1, [2]], # 15 64 80 80
  [4, 1, Split_2, [2]], # 16 64 80 80

  [12, 1, Conv, [128, 1, 1]], # 17 64 20 20
  [14, 1, Conv, [64, 1, 1]],  # 18 32 40 40
  [16, 1, Conv, [32, 1, 1]],  # 19 16 80 80

  [11, 1, nn.Upsample, [None, 2, 'nearest']], # 20 128 40 40
  [[-1, 13], 1, Concat, [1]], # 21 256 40 40
  [-1, 1, C3, [256, False]], # 22 128 40 40

  [-1, 1, Conv, [128, 1, 1]], # 23 64 40 40
  [-1, 1, nn.Upsample, [None, 2, 'nearest']], #24 64 80 80
  [[-1, 15], 1, Concat, [1]], # 25 128 80 80
  [-1, 1, C3, [128, False]], # 26 64 80 80

  [[19, 18, 17], 1, ASFF_3, [32, 0]],  #27 16 80 80
  [[-1, -2], 1, Concat, [1]], # 28 80 80 80
  [-1, 1, C3, [256, False]], # 29 128 80 80

  [26, 1, Conv, [128, 3, 2]], # 30 64 40 40
  [[-1, 20], 1, Concat, [1]], # 31 128 40 40
  [-1, 1, C3, [256, False]], # 32 128 40 40

  [[19, 18, 17], 1, ASFF_3, [64, 1]],  #33 32 40 40
  [[-1, -2], 1, Concat, [1]], # 34 160 40 40
  [-1, 1, C3, [512, False]], # 35 256 40 40

  [32, 1, Conv, [256, 3, 2]], # 36 128 20 20
  [[-1, 11], 1, Concat, [1]], # 37 256 20 20
  [-1, 1, C3, [256, False]], # 38 128 20 20
  
  [[19, 18, 17], 1, ASFF_3, [128, 2]],  #39 64 20 20
  [[-1, -2], 1, Concat, [1]], # 40 320 20 20
  [-1, 1, C3, [1024, False]], # 41 512 20 20

  [[29, 35, 41], 1, Detect, [nc, anchors]],
  ]
